# LuneX Wallet

An iOS SwiftUI prototype for a crypto wallet with a glassmorphism interface. It includes a portfolio dashboard, interactive period selector, asset market list, community chat composer, and profile/security surface.

## Open in Xcode

Open `LuneXWallet.xcodeproj`, select an iOS simulator running iOS 17 or newer, then build and run.
